# Практична робота №11-13

Проєкт створено за відповідною практичною роботою з Word/PDF-документа.

## Що реалізовано

- Форма калькулятора.
- `Panel`, `Label`, `TableLayoutPanel`, `Dock`, `MinimumSize`, вимкнені системні кнопки.

## Git

У репозиторій не додаються `bin/`, `obj/`, `.vs/` і `*.user`.
